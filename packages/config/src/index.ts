/**
 * @inception/config — Configuration Management
 * 
 * Global settings and environment bindings for the Creative Liberation Engine.
 */

export const config = {
    env: process.env.NODE_ENV || 'development'
};
