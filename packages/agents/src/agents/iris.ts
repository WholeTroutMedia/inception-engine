/**
 * @inception/agents — IRIS Agent Stub
 *
 * IRIS — Creative Director. UI generation. Design vision. Brand strategy execution.
 * Member of the AVERI Trinity.
 */

import type { AgentDefinition } from '../types.js';

export const IRIS: AgentDefinition = {
    id: 'IRIS',
    name: 'IRIS',
    description: 'Creative director — UI generation, design vision, brand strategy execution',
    hive: 'AVERI',
    modes: ['IDEATE', 'SHIP'],
    constitutionalAccess: true,
};
